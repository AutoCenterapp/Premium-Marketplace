import { ImageSortPipe } from './image-sort.pipe';

describe('ImageSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSortPipe();
    expect(pipe).toBeTruthy();
  });
});
